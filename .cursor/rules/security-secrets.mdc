---
description: 密钥与敏感信息安全规则
alwaysApply: true
---

# 安全与密钥

- 所有密钥仅通过环境变量或本地 `.env`（已 gitignore）。
- 可提交 `.env.example`，只含空占位键名。
- 禁止在代码、日志、PR 描述、commit message 中粘贴真实 token。
- 发现即将暂存的密钥文件必须停止并警告用户。
- Publisher 相关：`XHS_APP_SECRET`、`XHS_ACCESS_TOKEN`、`XHS_REFRESH_TOKEN`、`THIRD_PARTY_API_KEY` 不得入库。
