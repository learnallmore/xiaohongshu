{
  "version": 1,
  "hooks": {
    "beforeShellExecution": [
      {
        "command": "bash .cursor/hooks/block-dangerous-git.sh",
        "failClosed": true
      }
    ],
    "stop": [
      {
        "type": "prompt",
        "prompt": "Agent 即将结束本轮。必须读取并执行 .cursor/skills/auto-code-review/SKILL.md（固定五段输出）。若有 *.java 叠加 alibaba-java-coding-guidelines；若有 UI 对照 specs/007-frontend-design.md。确认：未自动 git push；未擅自 commit（除非用户要求）；无密钥；无非官方抓包发帖。有阻塞项用 followup_message 要求立刻修正。Hook 输入：$ARGUMENTS",
        "timeout": 30,
        "loop_limit": 2
      }
    ]
  }
}
